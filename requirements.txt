# Core dependencies for Totem project
# LED Matrix (MAX7219) control
luma.led-matrix>=1.7.0
luma.core>=2.4.0

# LCD (1602 I2C) control
RPLCD>=1.3.0

# Raspberry Pi GPIO and SPI
RPi.GPIO>=0.7.0
spidev>=3.5

# Image processing (required by luma)
Pillow>=8.1.0
